# Models

This directory is reserved for trained models, model configuration files, or model-loading artifacts if needed for submission.

The current project primarily uses external model APIs and local embedding-model loading rather than storing trained weights in-repo.
